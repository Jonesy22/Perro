import Button from 'react-bootstrap/Button'
import React, {useState, useEffect } from 'react'

const RightSummaryButton = ({}) => {

    return (
        <div>
            <Button variant="primary">Summary</Button>{' '}
        </div>
    );
}

export default RightSummaryButton
